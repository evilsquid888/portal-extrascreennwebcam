org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official
# Don't fail the build if Java/Kotlin JVM targets differ (built-in Kotlin defaults)
kotlin.jvm.target.validation.mode=warning
